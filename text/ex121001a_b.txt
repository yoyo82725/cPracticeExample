引入標頭檔stdio.h，因為要使用printf（）函數{印出}。
引入標頭檔stdlib.h，因為要使用system（）函數{呼叫Dos，使程式暫停}。
程式進入點，程式從這個地方開始跑。
大括號頭（必加）。
變數宣告；int是整數型態，int a=0是宣告整數a的值為0，後面b則只宣告無賦值。

印出變數a的值；printf（）中的%d是抓逗號後的數值印出，\n是換行。
同上，%d是印出逗號後變數b的值，\n是換行。
改變變數a的值為100
再次印出a的值，%d是以整數方式印出逗號後的值，\n是換行。
呼叫DOS指令，使程式停止。
回傳值，可以先不用知道。 回傳值是指此函數被呼叫時回傳的數值。
大括號尾（必加）。
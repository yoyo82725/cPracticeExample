引入標頭檔stdio.h，因為要使用printf（）函數{印出}。
引入標頭檔stdlib.h，因為要使用system（）函數{呼叫Dos，使程式暫停}。
引入標頭檔math.h，因為要使用sin（）函數{數學函數}。

程式進入點，程式從這個地方開始跑。
大括號頭（必加）。
印出"Hello!"這幾個字，\n是換行。
印出"sin（1）=0.841471"，%f是以浮點數方式印出sin（1），即','後面的東西。
呼叫DOS指令，使程式停止。
回傳值，可以先不用知道。 回傳值是指此函數被呼叫時回傳的數值。
大括號尾（必加）。